import { Injectable } from '@nestjs/common';
import { RecordInt } from './interfaces/record.interface'

@Injectable()
export class RecordService {
  create(recordInt: RecordInt) {
    
  }

  findOne(recordInt: RecordInt) {
      
  }
}
